if(TEST)
        add_subdirectory(../tests ./tests)
else()
        if(IS_GUI)
                add_subdirectory(machine)
                add_subdirectory(emulator)
                add_subdirectory(ui)
        else()
                add_subdirectory(machine)
                add_subdirectory(emulator)
        endif()
endif()